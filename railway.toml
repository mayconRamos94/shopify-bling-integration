[build]
builder = "NIXPACKS"

[deploy]
startCommand = "java -jar target/shopify-bling-1.0.0.jar"
healthcheckPath = "/logs/summary"
healthcheckTimeout = 60
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
